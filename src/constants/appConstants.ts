const appConstants = {
    // The name of the cookie that stores the user's authentication token
    authCookieName: 'iShop_auth',

    // The name of the cookie that stores the user's authentication token
    tokenCookieName: 'iShop_token',

    // The name of the cookie that stores the user's authentication token
    forgotPasswordCookieName: 'iShop_forgot_password',
};

export default appConstants;
